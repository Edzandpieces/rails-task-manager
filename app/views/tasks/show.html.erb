<div class="container">
  <h1><%= @task.title %></h1>
    <p><%= @task.details %></p>
    <p><%= @task.completed %></p>
  <%= link_to "Go back to task list", tasks_path %>
  <%= link_to "Delete me, baka!", task_path(@task), method: :delete, data: { confirm: "Are you sure?" } %>
</div>
